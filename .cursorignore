# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)

# Allow .env files to be indexed by Cursor (but they're still gitignored)
!.env*
!.env.local
!.env.development
!.env.production
